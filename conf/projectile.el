;;; conf/projectile.el -*- lexical-binding: t; -*-

(setq
 projectile-globally-ignored-directories '(
    "backup"
    "dist"
    "dist-newstyle"
    "result"
    ".marconidb"
    ".db"
    ".direnv"
    ".direnv-disable"))
